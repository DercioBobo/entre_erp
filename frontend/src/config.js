export const portalCtx = {
    customer:      window.portalCtx?.customer      ?? "",
    customerLabel: window.portalCtx?.customerLabel ?? "",
    user:          window.portalCtx?.user          ?? "",
    userFullname:  window.portalCtx?.userFullname  ?? "",
};
